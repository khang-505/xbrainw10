apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-fail-latest
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fail-latest
  template:
    metadata:
      labels:
        app: fail-latest
    spec:
      containers:
      - name: nginx
        image: nginx:latest