apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-fail-limits
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fail-limits
  template:
    metadata:
      labels:
        app: fail-limits
    spec:
      containers:
      - name: nginx
        image: nginx:1.21.6