apiVersion: v1
kind: Pod
metadata:
  name: test-violation-no-limits
  namespace: demo
  labels:
    app: test-limit
spec:
  containers:
  - name: nginx-container
    image: nginx:1.25-alpine
    resources:
      requests:
        memory: "64Mi"
        cpu: "250m"